blog.qixin.ch {
    # 路由块：优先处理 /refresh 请求
    route /refresh {
        # 反向代理到 Python Webhook 服务
        # 转发给 127.0.0.1:8080，这是上面 Python 脚本监听的地址
        reverse_proxy 127.0.0.1:18080
    }

    # 默认的静态文件服务
    root * /root/myblog/public/
    file_server
}
